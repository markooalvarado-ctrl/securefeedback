<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>SecureFeedback | Registros</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <style>
        body {
            font-family: Arial, Helvetica, sans-serif;
            background: #0f172a;
            margin: 0;
            padding: 40px 20px;
            color: #0f172a;
        }

        .container {
            max-width: 1000px;
            margin: auto;
        }

        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 25px;
        }

        .header h2 {
            color: #ffffff;
            margin: 0;
        }

        .header a {
            text-decoration: none;
            background: #2563eb;
            color: white;
            padding: 10px 16px;
            border-radius: 6px;
            font-size: 14px;
            font-weight: bold;
        }

        .header a:hover {
            background: #1e40af;
        }

        .card {
            background: #ffffff;
            border-radius: 10px;
            padding: 20px;
            box-shadow: 0 15px 40px rgba(0,0,0,0.3);
        }

        table {
            width: 100%;
            border-collapse: collapse;
            font-size: 14px;
        }

        thead {
            background: #2563eb;
            color: white;
        }

        th, td {
            padding: 12px 14px;
            text-align: left;
        }

        th {
            font-weight: bold;
            font-size: 13px;
            text-transform: uppercase;
        }

        tbody tr:nth-child(even) {
            background: #f1f5f9;
        }

        tbody tr:hover {
            background: #e0e7ff;
        }

        .empty {
            text-align: center;
            padding: 30px;
            color: #475569;
        }

        footer {
            text-align: center;
            margin-top: 20px;
            color: #94a3b8;
            font-size: 12px;
        }
    </style>
</head>

<body>

<div class="container">

    <div class="header">
        <h2>Registros de SecureFeedback</h2>
        <a href="/">➕ Nuevo registro</a>
    </div>

    <div class="card">

        <% if (users.length === 0) { %>
            <div class="empty">
                No existen registros almacenados.
            </div>
        <% } else { %>

        <table>
            <thead>
                <tr>
                    <th>Nombre</th>
                    <th>Correo electrónico</th>
                    <th>Comentario</th>
                    <th>Fecha</th>
                </tr>
            </thead>
            <tbody>
                <% users.forEach(u => { %>
                <tr>
                    <td><%= u.name %></td>
                    <td><%= u.email %></td>
                    <td><%= u.comment %></td>
                    <td>
                        <%= u.created_at 
                            ? new Date(u.created_at).toLocaleString() 
                            : '' 
                        %>
                    </td>
                </tr>
                <% }) %>
            </tbody>
        </table>

        <% } %>
    </div>

    <footer>
        GRUPO D
    </footer>

</div>

</body>
</html>
